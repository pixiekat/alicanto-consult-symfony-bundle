{% extends 'base.html.twig' %}

{% block title %}Editing Group {{ group.id}}: {{group.name}}{% endblock %}

{% block body %}
  <div class="container">
    <div class="row align-items-md-center">
      <div class="col-12">
        <h1>Editing Group {{ group.id}}: {{group.name}}</h1>
      </div>
    </div>
    <div class="row">
      <div class="col-12">
        {% if form is defined %}
          {{ form(form) }}
        {% endif %}
      </div>
    </div>
  </div>
{% endblock %}
